spring:
  application:
    name: authN
  datasource:
    driver-class: org.h2.Driver
    url: jdbc:h2:mem:testdb;Mode=MySQL;INIT=CREATE SCHEMA IF NOT EXISTS authn
    username: sa
    password: sa
  jpa:
    database: default
    hibernate:
      ddl-auto: none
jwt:
  secret: 'q@7}Z]k7D9#>5M(B`Qu?AJ8'

server:
  port: 8090